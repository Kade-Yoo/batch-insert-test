spring:
  config:
    activate:
      on-profile: test
  h2:
    console:
      enabled: true
  datasource:
    driver-class-name: org.h2.Driver
    username: sa
    # ???? PostgreSQL? ???? ?? ??? Mode? PostgreSQL? ????
    # DATABASE_TO_LOWER = TRUE -> ???? ?? ???? ?? ??? Lower Case? ???? ??
    # DEFAULT_NULL_ORDERING = HIGH -> ??? ? NULL ?? ??? ???? ?? ???? ??
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
  jpa:
    database: h2
    # JPA SQL log ?? ??
    show-sql: true
    defer-datasource-initialization: true
    # ???? ?? ?? default : create-drop?? ???? ??? ???? ???? ???? ?? ???
    # ?? sql.init.schema-locations, sql.init.data-locations? ??? ?? ddl-auto : none?? ??????
    hibernate:
      ddl-auto: none
  sql:
    init:
      schema-locations: classpath:schema-h2.sql
      data-locations: classpath:data-h2.sql
